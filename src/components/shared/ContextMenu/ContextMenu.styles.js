const styles = theme => ({
  root: {
    height: "100%",
    width: "auto",
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "center",
    flexDirection: "row",
    marginLeft: "-10px"
  }
});

export default styles;
