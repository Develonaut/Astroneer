const styles = theme => ({
  root: {
    display: "inline-grid",
    gridTemplateColumns: "89px 73px 44px 44px min-content",
    gridGap: "5px",
    alignItems: "center"
  }
});

export default styles;
