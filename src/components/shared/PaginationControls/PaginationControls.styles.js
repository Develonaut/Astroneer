const styles = theme => ({
  root: {
    display: "grid",
    whiteSpace: "nowrap",
    gridTemplateColumns: "min-content min-content min-content",
    gridGap: "10px",
    alignItems: "center",
    fontSize: "12px"
  }
});

export default styles;
