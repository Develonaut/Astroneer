const styles = theme => ({
  root: {
    ...theme.typography.bold,
    padding: "8px 27px",
    background: theme.palette.common.beam,
    color: theme.palette.common.sunshine,
    textTransform: "uppercase",
    borderRadius: "20px"
  }
});

export default styles;
