const styles = theme => ({
  root: {
    display: "inline-grid",
    gridTemplateRows: "min-content auto",
    gridTemplateColumns: "100%",
    width: "100%",
    height: "100%"
  },
  modal: {
    maxWidth: "700px",
    width: "700px"
  }
});

export default styles;
